FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY backend/requirements.txt .
RUN pip install --no-cache-dir flask flask-cors

# Copy backend code
COPY backend/ ./backend/

# Expose port
EXPOSE 8000

# Run the Flask server
CMD ["python", "backend/sep_server.py"]
